# CollisionAvoidance
University project.
Android application that "checks" if you are gonna collide with anything through sensors.
Talks with a server through MQTT(if more than 1 user registers a collision on the server simultaneously they are getting informed).
